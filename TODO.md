- スロープ
  - 物理演算実装
    - Sphere2Slope.space{Full,Half} の実装
      - 長方形とのspace計算を3回すれば良いかも
        - space計算は長方形とのspace計算で一般化できるかも
          - 「space関数の計算がかなり複雑なので、上手く一般化したいのですが、何か案はある？実行速度はなるべく犠牲にしたくない」
      - 「Sphere2RectPrism.spaceの実装を参考にして、Sphere2Slope.spaceFullの実装を完成させてください。」
  - playerの移動をminionと同じ実装方式にする
- update系の処理をGameに集約する
- カーソルを合わせたオブジェクトのデバッグ情報表示機能
  - 「デバッグモードがONのときに、画面上で、特定のEntity(PlayerまたはMinion)にカーソルを合わせた際に、そのEntityの詳細情報を画面に出し、かつそのEntityをハイライトするようにしたいです。どうすれば実現できるか検討してください。コードの変更はまだ行わないでください。」
- 衝突判定の高速化
  - 距離が近い場合にのみ衝突判定
  - n(n-1)/2 回の比較を避ける
