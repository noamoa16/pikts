- update系の処理をGameに集約する
- 「Minionごとにベクトルを少しずらして重ならないようにする」処理 (2箇所) はハッシュじゃなくてランダムの方がいいかも
  - 「src/objects/minion/minion.tsで、Minionごとにベクトルを少しずらして重ならないようにする処理が2箇所にあります。現状、this.hashを使って、オブジェクトごとに決まった値だけずらすようにしていますが、乱数でずらすようにしてください。ANGLE_NUMは不要になるはずで、MAX_ANGLEの範囲の実数値の乱数を生成して使うようにすればいいはずです。」
- 複数Minionを高速で投げると空中でMinion同士が衝突する問題の解消
  - 「複数Minionを高速で投げると、空中でMinion同士が衝突して着地判定になってしまう問題があります。この問題の解決策を提案してください。コードの変更はまだ行わないでください。」
- Blockの上に乗ったMinionがBlockの上から降りられない問題の解消
  - 「Blockの上にMinionを投げて乗せると、MinionがBlockから降りられなくなる問題があります。この問題の原因は何ですか？コードの変更はまだ行わないでください。」 
- 衝突判定の高速化
  - 距離が近い場合にのみ衝突判定
  - n(n-1)/2 回の比較を避ける
